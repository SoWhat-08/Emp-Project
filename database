-- ==========================================
-- WORKHUB EMPLOYEE MANAGEMENT SYSTEM
-- ==========================================

-- Create Database
CREATE DATABASE workhub;

-- Use Database
USE workhub;

-- ==========================================
-- USERS TABLE (REGISTER & LOGIN)
-- ==========================================

CREATE TABLE users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    full_name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL
);

-- Register Users

INSERT INTO users (full_name, email, password)
VALUES
('Admin', 'admin@workhub.com', 'admin123'),
('John David', 'john@gmail.com', 'john123'),
('Emma Wilson', 'emma@gmail.com', 'emma123');

-- View Registered Users

SELECT * FROM users;

-- Login Query

SELECT *
FROM users
WHERE email = 'admin@workhub.com'
AND password = 'admin123';

-- ==========================================
-- EMPLOYEE TABLE
-- ==========================================

CREATE TABLE employees (
    employee_id INT AUTO_INCREMENT PRIMARY KEY,
    first_name VARCHAR(50) NOT NULL,
    last_name VARCHAR(50) NOT NULL,
    gender ENUM('Male','Female','Other') NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    phone VARCHAR(15) NOT NULL,
    department VARCHAR(50) NOT NULL,
    designation VARCHAR(50) NOT NULL,
    salary DECIMAL(10,2) NOT NULL,
    joining_date DATE NOT NULL
);

-- ==========================================
-- INSERT EMPLOYEES
-- ==========================================

INSERT INTO employees
(first_name,last_name,gender,email,phone,department,designation,salary,joining_date)
VALUES
('John','David','Male','john.employee@gmail.com','9876543210','HR','HR Manager',45000,'2025-01-15'),
('Emma','Wilson','Female','emma.employee@gmail.com','9876543211','IT','Frontend Developer',60000,'2024-09-10'),
('Michael','Brown','Male','michael@gmail.com','9876543212','Finance','Accountant',55000,'2023-07-20'),
('Sophia','Taylor','Female','sophia@gmail.com','9876543213','Marketing','Marketing Executive',42000,'2025-03-12'),
('Daniel','Thomas','Male','daniel@gmail.com','9876543214','IT','Backend Developer',70000,'2024-11-05');

-- ==========================================
-- VIEW ALL EMPLOYEES
-- ==========================================

SELECT * FROM employees;

-- ==========================================
-- VIEW EMPLOYEE BY ID
-- ==========================================

SELECT *
FROM employees
WHERE employee_id = 1;

-- ==========================================
-- UPDATE EMPLOYEE
-- ==========================================

UPDATE employees
SET
    first_name = 'John',
    last_name = 'David',
    gender = 'Male',
    email = 'john.employee@gmail.com',
    phone = '9999999999',
    department = 'Finance',
    designation = 'Finance Manager',
    salary = 55000,
    joining_date = '2025-01-15'
WHERE employee_id = 1;

-- ==========================================
-- DELETE EMPLOYEE
-- ==========================================

DELETE FROM employees
WHERE employee_id = 1;

-- ==========================================
